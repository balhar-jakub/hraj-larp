<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"

       xsi:schemaLocation="
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-3.0.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
        http://www.springframework.org/schema/task
        http://www.springframework.org/schema/task/spring-task-3.0.xsd">

    <!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->

    <!-- Scans within the base package of the application for @Components to configure as beans -->
    <!-- @Controller, @Service, @Configuration, etc. -->
    <context:component-scan base-package="cz.hrajlarp" />

    <!-- Enables the Spring MVC @Controller programming model -->
    <mvc:annotation-driven />

    <!-- Mapping of static files -->
    <mvc:resources mapping="/img/**" location="/assets/img/" />
    <mvc:resources mapping="/style/**" location="/assets/style/" />
    <mvc:resources mapping="/js/**" location="/assets/js/" />
    <mvc:resources mapping="/html/**" location="/assets/html/" />
    <mvc:resources mapping="/fonts/**" location="/assets/fonts/" />

    <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource">
        <property name="driverClassName" value="org.postgresql.Driver" />

<!--
        <property name="url" value="jdbc:postgresql://194.8.253.136/hrajlarp" />
        <property name="username" value="hrajlarp" />
        <property name="password" value="5e5icha" />
-->

<!--
        <property name="url" value="jdbc:postgresql://localhost:5432/hrajlarp?characterEncoding=UTF-8" />
        <property name="username" value="postgres" />
        <property name="password" value="incorrect" />
-->
        <property name="url" value="jdbc:postgresql://194.8.253.136/sutrhraj?characterEncoding=utf-8" />
        <property name="username" value="sutr" />
        <property name="password" value="sutrDb" />
<!-->
        <property name="url" value="jdbc:postgresql://194.8.253.136/pasekhraj?characterEncoding=utf-8" />
        <property name="username" value="pasek" />
        <property name="password" value="pasekDb" />
-->
		<property name="initialSize" value="10" />
        <property name="maxActive" value="50" />
    </bean>

    <bean id="sessionFactory" class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <property name="packagesToScan" value="cz.hrajlarp" />
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">org.hibernate.dialect.PostgreSQL82Dialect</prop>
                <prop key="hibernate.hbm2ddl.auto">update</prop>
                <prop key="hibernate.show_sql">false</prop>
            </props>
        </property>
    </bean>

    <bean id="transactionManager" class="org.springframework.orm.hibernate4.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- resolver for uploading images -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="20971520" /> <!-- 20MB -->
        <property name="maxInMemorySize" value="1048576" />  <!-- 1MB -->
    </bean>
    
    <bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
  		<property name="host" value="smtp.gmail.com"/>
  		<property name="port" value="587"/>
        <property name="username" value="hrajlarp111@gmail.com"/>
        <property name="password" value="hrajlarp2013"/>
        <property name="javaMailProperties">
          <props>
           <prop key="mail.smtp.auth">true</prop>
       	   <prop key="mail.smtp.starttls.enable">true</prop>
          </props>
        </property>
	</bean>

	<bean id="templateMessage" class="org.springframework.mail.SimpleMailMessage">
	  	<property name="from" value="hrajlarp111@gmail.com"/>
	  	<property name="subject" value="HRAJLARP - Změna statusu u hry"/>
	</bean>
	
	<bean id="mailService" class="cz.hrajlarp.utils.MailService">
	  	<property name="mailSender" ref="mailSender"/>
	  	<property name="templateMessage" ref="templateMessage"/>
	</bean>

    <mvc:interceptors>
        <bean class="cz.hrajlarp.utils.HrajInterceptor" />
    </mvc:interceptors>

    <task:annotation-driven scheduler="taskScheduler"/>
    <task:scheduler id="taskScheduler" pool-size="2"/>
</beans>